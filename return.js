function doMath(num1, num2) {
    const sum = num1 + num2
    const diff = num1 - num2
    const mul = sum * diff
    const result = mul / 2
    return result
}
const result = doMath(100, 20)
console.log(result)